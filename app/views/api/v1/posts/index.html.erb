<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>ルビーのブログ</title>
</head>
<body>

<h1>rails初心者ブログ</h1>
<%= link_to '新規投稿', new_api_v1_post_path %>
<table>
    <thead>
        <tr>
            <th>タイトル</th>
            <th>内容</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <% @posts.each do |post|%>
    <tr>
        <td><%= post.title %></td>
        <td><%= post.content%></td>
        <td><%= link_to '詳細', api_v1_post_path(post) %></td>
        <td><%= link_to '編集', edit_api_v1_post_path(post) %></td>
        <td><%= link_to '削除', api_v1_post_path(post), method: :delete,data:{confirm: '本当に削除しますか？'} %></td>
    </tr>
    <% end %>
</table>

<br>

</body>
</html>